const Vli = require("./helpers/console/");

Vli.log("APP STARTED!");

const {
  V_TimeLine,
  Vtl,
  page_prev,
  page_next,
  page_run,
  push_page,
  replace_page,
} = require("./helpers/history/");
